#include<bits/stdc++.h>
using namespace std;
inline void fun(){
    cout<<endl<<"Small line of code in fun than call in main program multiple time than stack in same fun can multiple time use Memmory of stack than use inline fun than call multiple time than main program in every time pasted code in main program than number of call in stack is low than low Overhead compare to regular function call... "<<endl;
}
int main()
{
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();
    fun();


    
    return 0;
}